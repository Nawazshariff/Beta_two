<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nawazshariff.beta_two">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MainActivity"
            android:label="@string/app_name">
            </activity>
        <activity android:name=".Activities.Timeline_facility"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".Activities.Timeline"
            android:label="@string/app_name">

        </activity>
        <activity android:name=".Activities.ForgotPass"
            android:label="@string/app_name">

        </activity>
        <activity android:name=".Activities.CompleteProfile"
            android:label="@string/app_name">

        </activity>
        <activity android:name=".Activities.TimelineProfile"
            android:label="@string/app_name">

        </activity>
        <activity android:name=".Activities.InfoPage"
            android:label="@string/app_name">

        </activity>

    </application>

</manifest>